# Sensitive variables - NEVER commit actual values
# Copy to secrets.tfvars and add real values

# Example (when we need it later):
# db_password = "your-secure-password"
# api_key     = "your-api-key"
