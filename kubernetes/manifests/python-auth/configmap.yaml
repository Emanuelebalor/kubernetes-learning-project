apiVersion: v1
kind: ConfigMap
metadata:
  name: python-auth-config
  namespace: microservices
data:
  DB_HOST: "postgres.postgresql.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "taskdb"
  DB_USER: "taskuser"  # Non-sensitive, can stay in ConfigMap